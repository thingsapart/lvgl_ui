/* AUTO-GENERATED by the 'c_code' backend */

#include "lvgl.h"
#include "lvgl_ui.h"
#include <stdlib.h> // For malloc

// --- Custom Includes from API Spec ---
#include "ui/components/lv_cam_positioning.h"
#include "ui/components/lv_cam_stream.h"
#include "ui/components/lv_probing_wizard_stubs.h"
#include "ui/components/lv_probing_wizard.h"
#include "ui.h"

// --- Deferred UI Functions ---

static void create_tab_1(lv_obj_t* parent) {
    // unnamed: label_2 (label)
    lv_obj_t* label_2 = lv_label_create(parent);

    lv_label_set_text(label_2, "Tab 1 Content");
    lv_obj_set_style_align(label_2, LV_ALIGN_CENTER, 0);

}

static void create_tab_2(lv_obj_t* parent) {
    // unnamed: label_4 (label)
    lv_obj_t* label_4 = lv_label_create(parent);

    lv_label_set_text(label_4, "Tab 2 Content");
    lv_obj_set_style_align(label_4, LV_ALIGN_CENTER, 0);

}

static void create_tab_3(lv_obj_t* parent) {
    // unnamed: label_6 (label)
    lv_obj_t* label_6 = lv_label_create(parent);

    lv_label_set_text(label_6, "Tab 3 Active!");
    lv_obj_set_style_align(label_6, LV_ALIGN_CENTER, 0);

}

void create_ui(lv_obj_t* parent) {
    // unnamed: tabview_0 (tabview)
    lv_obj_t* tabview_0 = lv_tabview_create(parent);

    lv_obj_set_style_size(tabview_0, 320, 240, 0);
    lv_obj_center(tabview_0);
    // unnamed: obj_1 (obj)
    lv_obj_t* obj_1 = lv_tabview_add_tab(tabview_0, "Tab 1");

    create_tab_1(obj_1);

    // unnamed: obj_3 (obj)
    lv_obj_t* obj_3 = lv_tabview_add_tab(tabview_0, "Tab 2");

    create_tab_2(obj_3);

    // unnamed: obj_5 (obj)
    lv_obj_t* obj_5 = lv_tabview_add_tab(tabview_0, "Tab 3");

    create_tab_3(obj_5);

    lv_tabview_set_active(tabview_0, 2, LV_ANIM_OFF);

}
