/* AUTO-GENERATED by the 'c_code' backend */

#include "lvgl.h"
#include "c_gen/lvgl_dispatch.h" // For obj_registry_add
#include "data_binding.h"

void create_ui(lv_obj_t* parent) {
    // unnamed: tileview_0 (tileview)
    lv_obj_t* tileview_0 = lv_tileview_create(parent);

    lv_obj_set_style_size(tileview_0, lv_pct(100), lv_pct(100), 0);
    // unnamed: obj_1 (obj)
    lv_obj_t* obj_1 = lv_tileview_add_tile(tileview_0, 0, 0, LV_DIR_RIGHT);

    // unnamed: label_2 (label)
    lv_obj_t* label_2 = lv_label_create(obj_1);

    lv_label_set_text(label_2, "Tile 1: Swipe Right");
    lv_obj_center(label_2);


    // unnamed: obj_3 (obj)
    lv_obj_t* obj_3 = lv_tileview_add_tile(tileview_0, 1, 0, LV_DIR_LEFT);

    // unnamed: label_4 (label)
    lv_obj_t* label_4 = lv_label_create(obj_3);

    lv_label_set_text(label_4, "Tile 2: Swipe Left");
    lv_obj_center(label_4);



}
