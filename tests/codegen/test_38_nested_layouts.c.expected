/* AUTO-GENERATED by the 'c_code' backend */

#include "lvgl.h"
#include "lvgl_ui.h"
#include <stdlib.h> // For malloc

// --- Memory Allocator (define LVGL_UI_MALLOC/FREE before this file to override) ---
#ifndef LVGL_UI_MALLOC
  #if defined(ESP32_HW) && defined(BOARD_HAS_PSRAM)
    #include "esp_heap_caps.h"
    #define LVGL_UI_MALLOC(sz) heap_caps_malloc((sz), MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT)
    #define LVGL_UI_FREE(ptr)  heap_caps_free(ptr)
  #else
    #define LVGL_UI_MALLOC(sz) malloc((sz))
    #define LVGL_UI_FREE(ptr)  free(ptr)
  #endif
#endif
#ifndef LVGL_UI_FREE
  #define LVGL_UI_FREE(ptr) free(ptr)
#endif

// --- Custom Includes from API Spec ---
#include "ui/components/lv_cam_positioning.h"
#include "ui/components/lv_cam_stream.h"
#include "ui/components/lv_probing_wizard_stubs.h"
#include "ui/components/lv_probing_wizard.h"
#include "ui.h"

void create_ui(lv_obj_t* parent) {
    // unnamed: obj_0 (obj)
    lv_obj_t* obj_0 = lv_obj_create(parent);

    lv_obj_set_style_layout(obj_0, LV_LAYOUT_FLEX, 0);
    lv_obj_set_style_flex_flow(obj_0, LV_FLEX_FLOW_COLUMN, 0);
    lv_obj_set_style_size(obj_0, 250, 150, 0);
    lv_obj_set_style_pad_all(obj_0, 5, 0);
    lv_obj_set_style_pad_gap(obj_0, 5, 0);
    lv_obj_center(obj_0);
    // unnamed: obj_1 (obj)
    lv_obj_t* obj_1 = lv_obj_create(obj_0);

    lv_obj_set_style_layout(obj_1, LV_LAYOUT_FLEX, 0);
    lv_obj_set_style_flex_flow(obj_1, LV_FLEX_FLOW_ROW, 0);
    lv_obj_set_style_width(obj_1, lv_pct(100), 0);
    lv_obj_set_style_flex_grow(obj_1, 1, 0);
    // unnamed: button_2 (button)
    lv_obj_t* button_2 = lv_button_create(obj_1);

    lv_obj_set_style_flex_grow(button_2, 1, 0);


    // unnamed: obj_3 (obj)
    lv_obj_t* obj_3 = lv_obj_create(obj_0);

    lv_obj_set_style_layout(obj_3, LV_LAYOUT_FLEX, 0);
    lv_obj_set_style_flex_flow(obj_3, LV_FLEX_FLOW_ROW, 0);
    lv_obj_set_style_width(obj_3, lv_pct(100), 0);
    lv_obj_set_style_flex_grow(obj_3, 1, 0);
    // unnamed: slider_4 (slider)
    lv_obj_t* slider_4 = lv_slider_create(obj_3);

    lv_obj_set_style_flex_grow(slider_4, 1, 0);



}

void destroy_ui(lv_obj_t** root_ptr) {
    if (!root_ptr || !*root_ptr) return;
    lv_obj_delete(*root_ptr);
    *root_ptr = NULL;
}
