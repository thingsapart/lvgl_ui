# Tests how the parser handles invalid or malformed escape sequences.
# The parser should treat them as literal characters.
invalid: "Unknown escape: \\q, Short hex: \\u123, Bad hex: \\u123G, Trailing backslash at end: \\"
