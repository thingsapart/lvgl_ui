# A snippet from a Helm Chart's values.yaml file.
# Tests nested objects, dot-notation-like keys, and strings that look like numbers.
replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent
  tag: ""

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
