// Code generated by LVGL UI Generator

#include "lvgl.h"

// Forward declaration for any registered C pointers you might have from your application.
// Example: extern const lv_font_t my_custom_font;
void create_ui(lv_obj_t* parent) {
    {
        lv_obj_t* screen1_obj = lv_obj_create(parent);
        {
            lv_obj_t* label1_obj = lv_label_create(screen1_obj);
            lv_label_set_text(label1_obj, "Hello TypeCheck");
            // Property 'layout' (lv_obj_set_layout) expects lv_layout_t.
            // LV_ALIGN_CENTER is lv_align_t.
            lv_obj_set_style_layout(label1_obj, LV_ALIGN_CENTER);
            // Property 'align' (lv_obj_set_align) expects lv_align_t.
            // LV_DIR_BOTTOM is lv_dir_t.
            lv_obj_set_style_align(label1_obj, LV_DIR_BOTTOM);
            registry_add_pointer(ui_registry, label1_obj, "label1", "label");
        }
        {
            lv_obj_t* obj_with_call = lv_obj_create(screen1_obj);
            // Using lv_obj_add_flag which takes lv_obj_flag_t. LV_STATE_DEFAULT is lv_state_t
            lv_obj_add_flag(obj_with_call, LV_STATE_DEFAULT);
        }
        registry_add_pointer(ui_registry, screen1_obj, "screen1", "obj");
    }
}
