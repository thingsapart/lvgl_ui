Error: 'with' block missing 'do' object or 'do' is not an object.
Warning: Property 'text' for object type 'obj' (C var 'obj_1') not found in API spec. Skipping.
// Code generated by LVGL UI Generator

#include "lvgl.h"

// Forward declaration for any registered C pointers you might have from your application.
// Example: extern const lv_font_t my_custom_font;
void create_ui(lv_obj_t* parent) {
    {
        lv_style_t* s_style_with = (lv_style_t*)malloc(sizeof(lv_style_t));
        if (s_style_with != NULL) {
            memset(s_style_with, 0, sizeof(lv_style_t));
            lv_style_init(s_style_with);
        } else {
            fprintf(stderr, "Error: Failed to malloc for object %s of type %s\n", "s_style_with", "lv_style_t");
        }
        lv_style_set_bg_color(s_style_with, lv_color_hex(0xFF0000));
        lv_style_set_text_color(s_style_with, lv_color_hex(0x00FF00));
    }
    {
        lv_obj_t* l_label_with = lv_label_create(parent);
    }
    {
        lv_obj_t* obj_container_with = lv_obj_create(parent);
        {
            lv_obj_t* l_target_1 = lv_label_create(obj_container_with);
        }
        {
            lv_obj_t* b_target_1 = lv_button_create(obj_container_with);
        }
    }
    {
        lv_obj_t* with_style_applier = lv_obj_create(parent);
        // Test case 1: Apply style properties using 'with'
        lv_style_t* style_0 = &s_style_with;
        lv_style_set_bg_opa(style_0, 255);
        lv_style_set_radius(style_0, 5);
        lv_style_set_border_width(style_0, 0);
    }
    {
        lv_obj_t* with_label_modifier = lv_obj_create(parent);
        // Test case 2: Modify properties of a label using 'with'
        lv_label_set_text(l_label_with, "Updated Text via With");
        lv_label_set_long_mode(l_label_with, "SCROLL_CIRCULAR");
        lv_obj_set_style_align(l_label_with, "CENTER");
        lv_obj_set_style_x(l_label_with, 0);
    }
    {
        lv_obj_t* with_button_in_container_modifier = lv_obj_create(parent);
        // Test case 3: Apply properties to a different object type (button) within container
        lv_obj_set_style_width(b_target_1, 100);
        lv_obj_set_style_height(b_target_1, 50);
        lv_obj_set_style_align(b_target_1, "TOP_LEFT");
    }
    {
        lv_obj_t* with_null_do = lv_obj_create(parent);
        // Test case 4: 'do' is entirely null (should do nothing gracefully)
    }
    {
        lv_obj_t* with_empty_do = lv_obj_create(parent);
        // Test case 5: 'obj' is specified but 'do' is empty (should do nothing gracefully)
    }
    {
        lv_obj_t* with_func_call_obj_modifier = lv_obj_create(parent);
        // Test case 6: 'obj' as a function call (lv_obj_get_child to get the first child label of obj_container_with)
        lv_obj_t* obj_1 = lv_obj_get_child(obj_container_with, 0);
        lv_obj_set_style_align(obj_1, "BOTTOM_MID");
        lv_obj_set_style_width(obj_1, 150);
    }
}
