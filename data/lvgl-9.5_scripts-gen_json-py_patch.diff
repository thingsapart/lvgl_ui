diff --git a/scripts/gen_json/pycparser_monkeypatch.py b/scripts/gen_json/pycparser_monkeypatch.py
index 98adf303..90ca72ea 100644
--- a/scripts/gen_json/pycparser_monkeypatch.py
+++ b/scripts/gen_json/pycparser_monkeypatch.py
@@ -2,6 +2,7 @@
 
 import sys
 import os
+import re
 
 try:
     from pycparser import c_ast  # NOQA
@@ -374,48 +375,92 @@ class Enum(c_ast.Enum):
         else:
             docstring = ''
 
+        # Improved enum initializer evaluation.
+        # - Normalize simple C casts at the start of the expression.
+        # - Strip integer literal suffixes (u, U, l, L).
+        # - Substitute previously-evaluated enum identifiers before eval.
+        #
+        # Note: referencing an identifier that has NOT been defined earlier
+        # in the same enum is invalid in C. We therefore evaluate each
+        # enumerator in-order and substitute only identifiers that are
+        # already present in `member_namespace`. This avoids needing an
+        # iterative/deferred pass. Examples this covers:
+        #
+        # LV_FLEX_FLOW_ROW_WRAP = LV_FLEX_FLOW_ROW | (1 << 2)
+        # LV_FLEX_FLOW_ROW_REVERSE = LV_FLEX_FLOW_ROW | (1 << 3)
+        # LV_FLEX_FLOW_ROW_WRAP_REVERSE = (LV_FLEX_FLOW_ROW | (1 << 2)) | (1 << 3)
+        # LV_STYLE_NUM_BUILT_IN_PROPS = LV_STYLE_LAST_BUILT_IN_PROP + 1
+        # LV_OBJ_FLAG_SCROLL_CHAIN = LV_OBJ_FLAG_SCROLL_CHAIN_HOR | LV_OBJ_FLAG_SCROLL_CHAIN_VER
+        # LV_OBJ_FLAG_FLEX_IN_NEW_TRACK = LV_OBJ_FLAG_LAYOUT_1
+        # LV_SLIDER_MODE_NORMAL = LV_BAR_MODE_NORMAL
+        # (these are real examples of cases that previously emitted warnings)
+
         members = []
         value_num = 0
 
+        def _normalize_code(code):
+            # Remove a simple C cast only if it appears at the start,
+            # e.g. (uint32_t)0x10 -> 0x10. Do not remove grouping parens.
+            code = re.sub(r'^\s*\([A-Za-z_][A-Za-z0-9_ \t\*]*\)\s*', '', code)
+            # Remove integer literal suffixes like U, UL, L, etc.
+            code = re.sub(r'([0-9]+|0x[0-9A-Fa-f]+)[uUlL]+', r'\1', code)
+            return code
+
         for item in (self.values or []):
             item_dict = item.to_dict()
-            try:
-                code = generator.visit(item.value)
-
-                try:
-                    value = eval("bytearray([b'" + code + "'])[0]")
-                except:  # NOQA
-                    index = code.find('L')
-
-                    while index >= 1:
-                        if code[index - 1].isdigit():
-                            code = list(code)
-                            code.pop(index)
-                            code = ''.join(code)
 
-                        index = code.find('L', index + 1)
-
-                    value = eval(code, member_namespace)
+            # If there's no explicit initializer, use the sequential value
+            # and do not emit warnings for those members.
+            if item.value is None:
+                member_namespace[item_dict['name']] = value_num
+                item_dict['value'] = value_num
+                members.append(item_dict)
+                value_num += 1
+                continue
 
-                member_namespace[item_dict['name']] = value
+            code = generator.visit(item.value)
+            norm = _normalize_code(code)
 
-                value_num = value + 1
+            # Substitute any identifiers that are already known in
+            # member_namespace with their numeric values so expressions
+            # like "FOO | (1 << 2)" evaluate correctly.
+            tokens = re.findall(r"\b[A-Za-z_]\w*\b", norm)
+            for t in set(tokens):
+                if t in member_namespace:
+                    norm = re.sub(rf"\b{re.escape(t)}\b", str(member_namespace[t]), norm)
 
-                code = f'0x{hex(value)[2:].upper()}'
-                value = code
-            except:  # NOQA
-                value = f'0x{hex(value_num)[2:].upper()}'
+            try:
+                val = eval(norm, {}, {})
+                int_val = int(val)
+                member_namespace[item_dict['name']] = int_val
+                item_dict['value'] = int_val
+                members.append(item_dict)
+                value_num = int_val + 1
+            except Exception:
+                # Emit a single warning when an explicit initializer fails
+                # to evaluate and fall back to the sequential value.
+                sys.stderr.write(
+                    f'WARNING: Could not evaluate enum initializer "{code}" for {item_dict["name"]} - using sequential fallback {value_num}\n'
+                )
                 member_namespace[item_dict['name']] = value_num
+                item_dict['value'] = value_num
+                members.append(item_dict)
                 value_num += 1
 
-            item_dict['value'] = value
-            members.append(item_dict)
-
-        hex_len = len(hex(value_num)[2:])
+        # Format all member values as zero-padded hex strings matching
+        # the width of the largest value.
+        hex_len = len(hex(value_num - 1)[2:]) if value_num > 0 else 1
         for member in members:
-            member['value'] = (
-                f'0x{hex(int(member["value"], 16))[2:].zfill(hex_len).upper()}'
-            )
+            member_val = member['value']
+            if isinstance(member_val, str):
+                try:
+                    iv = int(member_val, 16)
+                except Exception:
+                    iv = int(member_val)
+            else:
+                iv = int(member_val)
+
+            member['value'] = f'0x{hex(iv)[2:].zfill(hex_len).upper()}'
 
         res = OrderedDict([
             ('name', self.name),
